---
trigger: always_on
---

# Worktree Rules Disabled

Worktrees are currently disabled due to workflow issues. 

1. **DO NOT** use or attempt to set up git worktrees. 
2. **DO NOT** use the `setup-worktree` workflow.
3. Perform all git operations and code edits directly in the main repository directory.
4. Ensure you pull the latest changes from `main` before starting development.
